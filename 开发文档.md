# 类 Typora Markdown 笔记软件 —— 开发文档

> 目标：基于 **Tauri 2** 框架，开发一款高度还原 Typora 使用体验的跨平台 Markdown 笔记软件。
> 核心理念：**所见即所得（WYSIWYG）的"无缝实时预览"**——没有分屏预览，你编辑的就是最终排版效果，但底层始终是一份纯净的 Markdown 源文件。

---

## 目录

1. [项目概述](#1-项目概述)
2. [调研结论](#2-调研结论)
3. [需求分析（功能清单与优先级）](#3-需求分析功能清单与优先级)
4. [技术选型](#4-技术选型)
5. [总体架构设计](#5-总体架构设计)
6. [核心难点与技术方案](#6-核心难点与技术方案)
7. [目录结构](#7-目录结构)
8. [分阶段开发计划](#8-分阶段开发计划)
9. [Tauri 2 配置要点](#9-tauri-2-配置要点)
10. [风险与挑战](#10-风险与挑战)
11. [平台差异设计（Windows / macOS）](#11-平台差异设计windows--macos)
12. [UI 设计原则（干净、传统、去 AI 感）](#12-ui-设计原则干净传统去-ai-感)
13. [参考资料](#13-参考资料)

---

## 1. 项目概述

### 1.1 要做什么

一款类似 Typora 的桌面 Markdown 编辑器，核心特征：

- **无缝实时预览**：输入 Markdown 语法（如 `#`、`**`、`-`）时，语法标记立即"消失"，内容实时渲染成对应排版（标题、加粗、列表……），光标仍停留在正确位置。
- **纯文本为唯一数据源**：`.md` 文件就是真相，随时可用 `Ctrl+/` 切换到源码模式看到原始文本，二者可无缝来回切换，不丢格式。
- **极简、专注**：可隐藏侧边栏、专注模式、打字机模式。
- **跨平台**：Windows / macOS / Linux。

### 1.2 不做的事（明确边界，避免范围蔓延）

- 不做云端同步 / 多人协作（V1 阶段只做本地文件）。
- 不做双链/图谱（那是 Obsidian 的定位，不是 Typora）。
- 不做插件市场（V1 只做内置主题 + 自定义 CSS）。

### 1.3 与 Typora 的关键差异

| 维度 | Typora | 本项目 |
| --- | --- | --- |
| 外壳 | Electron / macOS 原生 WebView | **Tauri 2（系统 WebView）** |
| 包体 | ~100 MB+ | **~5–15 MB** |
| 内存 | ~300–500 MB | **~50–100 MB** |
| 编辑器内核 | CodeMirror（闭源魔改） | **CodeMirror 6（开源可定制）** |
| 数据源 | 纯 Markdown | 纯 Markdown（保持一致） |

---

## 2. 调研结论

> 本节汇总调研结果，作为后续技术决策的依据。详细来源见文末[参考资料](#11-参考资料)。

### 2.1 Typora 的实现机制（据其 credits 清单与应用包分析，闭源只能侧写）

- **外壳**：Windows/Linux 用 Electron，macOS 用原生 WebView；UI 层用 HTML/CSS/JS，依赖 Bootstrap、jQuery、React 等。
- **编辑器内核**：**CodeMirror**（`codemirror/mode.min.js` 被描述为"驱动 WYSIWYG 编辑、语法高亮与渲染的 Markdown 编辑器引擎"）。
- **公式**：MathJax 3。
- **渲染模型**：
  1. 输入实时解析为 AST，**只重渲染受影响的节点**（增量渲染）。
  2. 输入 `# ` 等标记 → 立即隐藏 `#` 字符、应用标题样式、光标保持原位。
  3. 光标进入某块（如标题、公式）→ 该块**临时展开回源码**；光标离开或回车 → 折叠回预览样式（通过 CSS 类切换 + 精确坐标计算实现）。
  4. 维护一张**"虚拟光标位置"映射表**，把视觉位置翻译回 Markdown 源码偏移量，保证在渲染态下的增删改符合直觉。

> **关键启示**：Typora 的"无缝预览"本质是"**装饰器 + 光标状态机**"，而非"编辑富文本"。源码永远不被替换，渲染只是视觉层。

### 2.2 MarkText（最著名的开源 Typora 替代品）

- Electron + Vue/Vuex + 自研 **Muya** 引擎。
- Muya 用"块(block)内容模型"做实时预览，`preBlock`（源码）+ `preview`（`editable:false` 的渲染块）双结构，用 **Snabbdom 虚拟 DOM 做局部 diff 更新**。
- CodeMirror 仅用于源码模式，且"功能弱、优化差"。

> **启示**：自研引擎（Muya 路线）工程量大、难以维护。CodeMirror 6 生态已能覆盖同样能力，不必自研。

### 2.3 现代编辑器库生态

| 方案 | 原理 | 优势 | 劣势 | 适用场景 |
| --- | --- | --- | --- | --- |
| **CodeMirror 6 + decorations**（推荐） | 纯文本为真相，用装饰器隐藏语法/内联渲染 | 最贴近 Typora；**Obsidian 实时预览即此方案**，久经考验；源码/预览无缝切换 | 需自行实现装饰器逻辑 | **本项目首选** |
| **Milkdown**（ProseMirror + remark） | 结构化文档，markdown 序列化保存 | 开箱即用的富文本交互（工具栏、斜杠菜单、图片上传）；插件生态 | markdown 往返有损、不符合"源码即真相"哲学、定制渲染需深入 ProseMirror | 备选 / 若未来要做富文本协作 |
| 分屏源码 + 预览 | 两个面板 | 实现最简单 | **完全不是 Typora 的体验** | 仅作原型验证 |

**结论**：选 **CodeMirror 6 + decorations**。它是"复刻 Typora 一模一样"的最短且最可靠的路径。

---

## 3. 需求分析（功能清单与优先级）

### 3.1 优先级定义

- **P0（MVP 必须）**：没有它就不是 Typora。
- **P1（重要）**：核心写作体验完整。
- **P2（增强）**：锦上添花，可后置。

### 3.2 功能清单

#### 编辑与实时渲染（核心）

| 功能 | 优先级 | 说明 |
| --- | --- | --- |
| 无缝实时预览（WYSIWYG） | **P0** | 标记即时隐藏、内容即时渲染 |
| 源码/预览模式切换（`Ctrl+/`） | **P0** | 双向无损 |
| 标题 H1–H6 | **P0** |  |
| 加粗 / 斜体 / 删除线 / 行内代码 | **P0** |  |
| 无序 / 有序 / 任务列表 | **P0** | `- [ ]` 可勾选 |
| 引用块 | **P0** |  |
| 代码块 + 语法高亮 | **P0** | 围栏 ``` |
| 表格（GFM） | **P0** | 行列增删、单元格导航 |
| 链接 / 图片 | **P0** | 图片本地/剪贴板粘贴 |
| 分割线 | **P0** |  |
| 脚注 | P1 |  |
| 自动配对标点 / 列表自动续行 | P1 |  |
| 块级拖拽调整顺序 | P2 |  |

#### 公式与图表

| 功能 | 优先级 | 说明 |
|---|---|---|
| 行内 / 块级 LaTeX 公式 | P1 | KaTeX 渲染 |
| Mermaid 图表 | P2 | 流程图、时序图等 |

#### 文件管理

| 功能 | 优先级 | 说明 |
| --- | --- | --- |
| 打开单个 .md 文件 | **P0** |  |
| 保存 / 另存为 | **P0** |  |
| 自动保存 | **P0** | 可配置间隔 + 失焦保存 |
| 打开文件夹 + 文件树侧边栏 | P1 | 新建/重命名/删除 |
| 多标签页 | P1 |  |
| 最近打开列表 | P1 |  |
| 外部文件变更监听 | P1 | 文件被外部修改时提示重载 |
| YAML Front Matter | P2 | 元信息编辑 |

#### 导出与导入

| 功能 | 优先级 | 说明 |
|---|---|---|
| 导出 HTML | P1 | 所见即所得 |
| 导出 PDF | P1 | 带书签 |
| 导出图片（PNG） | P2 | |
| 导出 DOCX / EPUB 等 | P2 | 依赖 Pandoc |

#### 界面与体验

| 功能 | 优先级 | 说明 |
| --- | --- | --- |
| 大纲（TOC）侧边栏 | P1 | 标题树 + 滚动同步 |
| 字数统计 | P1 | 字数/行数/阅读时长 |
| 主题系统（亮/暗 + 自定义 CSS） | P1 | 兼容 Typora 主题思路 |
| 专注模式 / 打字机模式 | P1 |  |
| 快捷键体系 | **P0** | 对齐 Typora |
| 系统托盘 / 全局快捷键 | P2 |  |

---

## 4. 技术选型

### 4.1 总览

| 层 | 选择 | 理由 |
| --- | --- | --- |
| 桌面框架 | **Tauri 2** | 包体小、内存低、Rust 安全、插件化 |
| 前端框架 | **React 19 + TypeScript + Vite** | 编辑器库生态最全；TS 保证大型编辑器代码可维护 |
| 编辑器内核 | **CodeMirror 6**（`@codemirror/state` + `@codemirror/view` + `@codemirror/lang-markdown` + `@lezer/markdown`） | 复刻 Typora 的最优解（见 2.3） |
| Markdown 语法解析 | **`@lezer/markdown`**（编辑器内，增量）+ **`unified/remark/rehype`**（导出渲染） | 前者提供增量语法树驱动装饰器；后者提供统一、可扩展的 HTML 生成 |
| 公式渲染 | **KaTeX** | 比 MathJax 快、包体小，效果足够 |
| 图表渲染 | **Mermaid** | 事实标准 |
| 代码高亮 | **highlight.js**（首选）/ shiki（备选） | hljs 轻量；shiki 更美观但更重，可后换 |
| 状态管理 | **Zustand**（轻量）/ Redux Toolkit（重） | 推荐 Zustand，够用且简单 |
| 样式 | **CSS 变量 + CSS Modules** | 便于做主题系统 |

> **备选方案**：若团队更熟 Vue，可用 Vue 3 + `@milkdown/vue`（或 `@uiw/react-codemirror` 的 Vue 等价物）。但 React 生态下 CodeMirror/Milkdown 的封装更成熟，默认推荐 React。

### 4.2 关键依赖清单

```jsonc
// package.json（前端，节选）
{
  "dependencies": {
    // CodeMirror 6 核心
    "@codemirror/state": "^6",
    "@codemirror/view": "^6",
    "@codemirror/language": "^6",
    "@codemirror/lang-markdown": "^6",
    "@codemirror/commands": "^6",
    "@codemirror/search": "^6",
    "@lezer/markdown": "^1",
    "@lezer/highlight": "^1",

    // 渲染
    "katex": "^0.16",
    "mermaid": "^11",
    "highlight.js": "^11",

    // 统一解析/序列化（导出用）
    "remark-parse": "^11",
    "remark-gfm": "^4",
    "remark-rehype": "^11",
    "rehype-stringify": "^10",
    "remark-frontmatter": "^5",

    // 前端
    "react": "^19",
    "react-dom": "^19",
    "zustand": "^5",

    // Tauri 插件 JS 侧
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-fs": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "@tauri-apps/plugin-clipboard-manager": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "@tauri-apps/plugin-store": "^2",
    "@tauri-apps/plugin-window-state": "^2",
    "@tauri-apps/plugin-log": "^2"
  }
}
```

```toml
# src-tauri/Cargo.toml（节选）
[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# 官方插件
tauri-plugin-fs = "2"
tauri-plugin-dialog = "2"
tauri-plugin-clipboard-manager = "2"
tauri-plugin-opener = "2"
tauri-plugin-store = "2"
tauri-plugin-window-state = "2"
tauri-plugin-log = "2"
tauri-plugin-updater = "2"   # P2 自动更新
```

### 4.3 为什么把 Markdown 解析分两层

- **编辑器内**用 `@lezer/markdown`：它输出**增量语法树**（`syntaxTree`），且与 CodeMirror 深度集成，能精确给出每个语法标记（`#`、`**`、围栏）的源码区间，这正是做"隐藏标记"装饰器需要的信息。**不要**在编辑器内用 remark（它是全量解析，每次输入都重建整棵树，大文档会卡）。
- **导出/预览渲染**用 `unified/remark/rehype`：它把 Markdown 转成标准 HTML，插件丰富（GFM、脚注、数学、mermaid），保证"导出的 HTML 就是你在编辑器里看到的样子"。

---

## 5. 总体架构设计

### 5.1 进程模型

```
┌─────────────────────────────────────────────────────────────┐
│                     Tauri 2 应用                             │
│                                                             │
│  ┌────────────────────────┐   ┌───────────────────────────┐ │
│  │  前端 WebView（React）  │   │   Rust 后端（核心进程）     │ │
│  │                        │   │                           │ │
│  │  · 编辑器 UI            │   │  · 文件读写（fs 插件）      │ │
│  │  · CodeMirror 6 编辑器   │   │  · 目录树/监听（watch）    │ │
│  │  · decorations 渲染     │   │  · 导出管线（Pandoc/打印）  │ │
│  │  · 主题/大纲/侧边栏      │◄──►│  · 设置持久化（store）     │ │
│  │  · KaTeX/Mermaid 渲染   │ IPC │  · 系统集成（托盘/快捷键） │ │
│  └────────────────────────┘   └───────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

- **前端（WebView）**：所有编辑与渲染逻辑。CodeMirror 6 跑在 WebView 里（它就是为 Web 环境设计的）。
- **Rust 后端**：一切需要系统权限的事——文件系统、原生对话框、导出、系统集成。通过 `invoke` 命令与前端通信。

> **职责边界原则**：凡是纯 UI/编辑的事放前端；凡是碰磁盘/系统 API 的事放 Rust。Markdown 解析放前端（配合 CodeMirror），文件 IO 放 Rust。

### 5.2 单一数据源（Single Source of Truth）

这是整个软件能否"像 Typora"的命脉：

```
               ┌─────────────────────────┐
               │    Markdown 纯文本字符串    │  ← 唯一真相
               └────────────┬────────────┘
                            │
              ┌─────────────┼─────────────┐
              ▼             ▼             ▼
        ┌──────────┐  ┌──────────┐  ┌──────────┐
        │ 编辑器    │  │ 大纲      │  │ 导出      │
        │ (CM6)    │  │ (TOC)    │  │ (HTML/PDF)│
        │ decorations│ │ 从语法树 │  │ remark    │
        │ 渲染      │  │ 提取标题  │  │ 管线      │
        └──────────┘  └──────────┘  └──────────┘
```

- 编辑器内部始终持有**纯 Markdown 字符串**，所有视觉效果都是"装饰"（decoration），不改源码。
- 大纲、导出都从这个字符串（或其语法树）派生，保证一致性。
- 保存 = 把这个字符串写回 `.md` 文件。

### 5.3 模块划分（前端）

```
Editor（编辑器核心）
 ├─ editorState      CodeMirror 状态管理
 ├─ decorations      实时预览装饰器（本项目的核心代码）
 ├─ commands         自定义命令（表格、列表、配对）
 ├─ plugins          扩展（搜索替换、历史、自动补全）
 └─ extensions       语言包、主题扩展

Render（渲染层）
 ├─ math             KaTeX 封装
 ├─ diagram          Mermaid 封装
 ├─ code             highlight.js 封装
 └─ export           remark→HTML、打印→PDF

UI（界面层）
 ├─ Sidebar          文件树 / 大纲
 ├─ Tabs             多标签
 ├─ Toolbar/StatusBar 字数统计、模式切换
 └─ Theme            主题系统

Bridge（桥接层）
 ├─ fsCommands       调 Rust 的文件命令封装
 └─ store            设置读写

Rust 后端
 ├─ file commands    读写/监听/目录树
 ├─ export           导出（可选调 Pandoc）
 └─ config           设置持久化
```

---

## 6. 核心难点与技术方案

> 这是本项目真正需要投入精力的地方。Typora 的"手感"几乎全部来自这几个问题的解法。

### 6.1 无缝实时预览（最核心）

**思路**：用 CodeMirror 6 的 **Decoration** 机制，把"渲染"变成"装饰"。

- `Decoration.replace({})` —— 用空内容**隐藏**语法标记（如 `#`、`**`、`` ` ``）。
- `Decoration.mark({ class, tagName })` —— 给内容区域**应用样式**（标题变大加粗、斜体倾斜等），或用 `tagName` 替换语义标签。
- `WidgetType` —— 在代码块、公式、表格处插入**渲染好的 DOM 块**（`editable: false`），显示语法高亮/公式/图表。

**实现骨架**（伪代码）：

```ts
import { ViewPlugin, Decoration } from "@codemirror/view";
import { syntaxTree } from "@codemirror/language";

// 一个 StateField 缓存 decorations，输入变化时增量重建
const livePreview = ViewPlugin.fromClass(class {
  decorations = Decoration.none;

  constructor(view) { this.build(view); }

  update(update) {
    if (update.docChanged || update.selectionSet) this.build(update.view);
  }

  build(view) {
    const decos = [];
    const tree = syntaxTree(view.state);
    const cursor = view.state.selection.main.head;

    tree.iterate({
      enter(node) {
        // 标题：隐藏 Markdown 标记，应用 heading 样式
        if (node.name === "ATXHeading1") {
          const mark = node.node.getChild("HeaderMark");
          if (mark && !在光标编辑范围内(mark, cursor)) {
            decos.push(Decoration.replace({}).range(mark.from, mark.to));
            decos.push(Decoration.mark({
              class: "md-heading md-heading-1"
            }).range(node.from, node.to));
          }
        }
        // 加粗：隐藏 ** ，加粗字体
        if (node.name === "StrongEmphasis") {
          // 隐藏开/闭标记（光标不在标记内时）
          // 给内容加 <strong> 或 .strong 样式
        }
        // 代码块：插入渲染后的高亮 DOM
        if (node.name === "FencedCode") {
          decos.push(Decoration.replace({
            widget: new CodeBlockWidget(node)
          }).range(node.from, node.to));
        }
        // ... 列表标记、引用、行内代码、链接等类似处理
      }
    });

    this.decorations = Decoration.set(decos, true);
  }
}, {
  decorations: v => v.decorations
});
```

**关键细节：光标感知（caret-aware reveal）**

隐藏标记时，必须判断光标是否落在标记区间内：

- 光标**不在**标记内 → 隐藏标记（预览态）。
- 光标**进入**标记区间 → 立刻展开标记（源码态），让用户能编辑原始 `#`/`**`。

这正是 Typora "光标进去展开、离开折叠"的效果，用 `cursor` 与标记的 `[from, to]` 做区间判断即可实现。

### 6.2 光标位置映射

在渲染态下，用户看到的"视觉位置"和源码的"字符偏移"不一致（因为 `#`、`**` 被隐藏）。CodeMirror 6 的 Decoration 机制**原生处理了这一点**——它自动维护视觉坐标与文档偏移的映射，所以：

- 按方向键、Backspace、Delete 时，CodeMirror 自动跳过/处理被 `replace` 的标记。
- 大多数情况无需手写"虚拟光标映射表"（Typora 因自研才需要）。

> **我们相对 Typora 的简化红利**：CodeMirror 6 把最难的映射问题解决了，我们只需专注"哪些区间隐藏/应用什么样式"。

**仍需手写的部分**：某些"整块替换"的渲染块（如公式块、表格预览态），需要处理"点击渲染块 → 回到对应源码行"的反向定位，用 `view.posAtCoords({x, y})` 实现。

### 6.3 大文档性能

- **增量解析**：`@lezer/markdown` 天然增量，输入只重解析改动段。
- **装饰器增量重建**：只在 `docChanged`/`selectionSet` 时重建，且只遍历可见视口（`view.visibleRanges`）对应的语法树区间。
- **视口虚拟化**：CodeMirror 只挂载可见 DOM，长文档（万字级）无压力。
- **重渲染缓存**：代码高亮、公式、图表只对可见块渲染，滚出视口即卸载（懒渲染）。

### 6.4 表格编辑

GFM 表格是 Typora 体验的重点，也是 CodeMirror 6 里最需要定制的部分：

- `@codemirror/lang-markdown` 已识别表格语法。
- 做表格的**预览态渲染**（隐藏 `|` 分隔线、应用表格样式）——可用 `WidgetType` 或 CSS 处理。
- 实现**行列操作命令**：`Tab` 在单元格间跳转、`Enter` 新增行、快捷键插入/删除行列。
- 进阶：单元格对齐、列宽自适应（P1）。

> 可参考开源库 `@codemirror/lang-markdown` 的表格实现与社区 `codemirror-table` 类插件。

### 6.5 数学公式（KaTeX）

- 行内 `$...$`、块级 `$$...$$`。
- 在 `@lezer/markdown` 的语法树上识别 `InlineCode`/`FencedCode` 节点判断是否公式。
- 编辑态：显示源码 `$...$`；预览态：用 KaTeX 渲染成 HTML 嵌入 `WidgetType`（块级）或 `Decoration.mark`（行内）。
- 注意 KaTeX 渲染开销，配合 6.3 的懒渲染。

### 6.6 图片粘贴 / 拖拽

- 剪贴板图片 → 读 `clipboard` 事件里的 `Blob` → 存到本地图片目录（或与 `.md` 同目录）→ 插入 `![](相对路径)`。
- 拖拽图片文件 → 读文件路径 → 复制或引用。
- 网络图片保留 URL 直接插入。
- 相对路径解析：以当前 `.md` 文件所在目录为基准。

### 6.7 外部文件变更监听

- Rust 侧用 `tauri-plugin-fs` 的 watch 能力（或 `notify` crate）监听当前文件/文件夹。
- 文件被外部修改 → 前端弹提示"文件已在外部被修改，是否重新加载"（对齐 Typora 行为），避免静默覆盖。

### 6.8 导出实现

| 格式 | 方案 | 说明 |
|---|---|---|
| HTML | `remark→rehype→stringify` + 内嵌主题 CSS | 所见即所得 |
| PDF | 方案 A：WebView 打印到 PDF（Chromium 打印）；方案 B：调用 Pandoc | 方案 A 保真度最高、无需外部依赖，首选 |
| DOCX/EPUB 等 | Pandoc（可选外部依赖） | 提供"已安装 Pandoc 则启用"的降级逻辑 |

> PDF 保真度是硬骨头。建议 V1 用"打印到 PDF"（复用浏览器引擎，和屏幕渲染一致），Pandoc 作为进阶增强。

---

## 7. 目录结构

```
InkNote/（项目根）
├── package.json
├── vite.config.ts
├── index.html
├── src/                          # 前端（React + TS）
│   ├── main.tsx
│   ├── App.tsx
│   ├── components/
│   │   ├── Editor/               # 编辑器外壳 + CodeMirror 集成
│   │   ├── Sidebar/              # 文件树 / 大纲
│   │   ├── Tabs/
│   │   ├── StatusBar/
│   │   └── Toolbar/
│   ├── editor/                   # 编辑器核心（本项目重点）
│   │   ├── livePreview.ts        # 6.1 装饰器逻辑
│   │   ├── widgets/              # CodeBlockWidget / MathWidget / MermaidWidget
│   │   ├── commands/             # 表格、列表等命令
│   │   └── extensions.ts         # 组装所有 CM6 扩展
│   ├── render/                   # 渲染层
│   │   ├── math.ts               # KaTeX
│   │   ├── diagram.ts            # Mermaid
│   │   ├── code.ts               # highlight.js
│   │   └── export/               # remark 管线 + 打印
│   ├── store/                    # Zustand 状态
│   ├── bridge/                   # Tauri invoke 封装
│   └── styles/                   # 主题 CSS
├── themes/                       # 内置主题（CSS）
└── src-tauri/                    # Rust 后端
    ├── Cargo.toml
    ├── tauri.conf.json
    ├── capabilities/             # 权限声明（重要！）
    │   └── default.json
    └── src/
        ├── main.rs
        ├── lib.rs
        ├── commands/
        │   ├── file.rs           # 读写/目录树/监听
        │   ├── export.rs         # 导出
        │   └── config.rs         # 设置
        └── models.rs
```

---

## 8. 分阶段开发计划

### M0 — 项目脚手架（0.5–1 天）

- [ ] `npm create tauri-app@latest` 初始化（React + TS + Vite 模板）。
- [ ] 接入 CodeMirror 6 基础编辑（源码模式可用）。
- [ ] 配置 Tauri 2 权限（capabilities）与基础插件。
- **验收**：能打开/保存 `.md` 文件，编辑器能高亮 Markdown。

### M1 — 核心实时预览 MVP（1–2 周）

- [ ] 实现 6.1 装饰器：标题、加粗、斜体、删除线、行内代码、引用、列表、代码块高亮。
- [ ] 实现 `Ctrl+/` 源码/预览切换。
- [ ] 自动保存、文件树（单文件 + 文件夹模式）。
- **验收**：写一篇含常见语法的文档，体验接近 Typora 的基础编辑。

### M2 — 写作体验补全（1–2 周）

- [ ] 表格编辑（6.4）、任务列表勾选、自动配对、列表续行。
- [ ] KaTeX 公式、图片粘贴/拖拽。
- [ ] 大纲 TOC + 滚动同步、字数统计。
- [ ] 主题系统（亮/暗 + 自定义 CSS）。
- **验收**：能完成一篇带公式/表格/图片的技术长文。

### M3 — 导出与增强（1–2 周）

- [ ] 导出 HTML / PDF（打印方案）。
- [ ] Mermaid 图表（P2）。
- [ ] 外部文件变更监听、多标签页、最近文件。
- [ ] 专注模式 / 打字机模式。
- **验收**：导出的 PDF/HTML 与编辑器内显示一致。

### M4 — 打磨与发布（1–2 周）

- [ ] 性能优化（大文档、懒渲染）。
- [ ] 快捷键完整对齐、无障碍、国际化。
- [ ] 打包发布（`tauri build`）、签名、自动更新（updater 插件）。
- **验收**：三平台安装包 + 一份使用说明。

---

## 9. Tauri 2 配置要点

> Tauri 2 最大的"坑"是**权限系统**：所有能力默认拒绝，必须在 `capabilities/*.json` 显式授权，否则命令静默失败。

### 9.1 capabilities 示例

```jsonc
// src-tauri/capabilities/default.json
{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力",
  "windows": ["main"],
  "permissions": [
    "core:default",
    // 文件系统（按需最小化授权，避免过度开放）
    "fs:default",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "fs:allow-exists",
    "fs:allow-read-dir",
    "fs:allow-create-dir",
    "fs:allow-remove",
    "fs:allow-rename",
    // 对话框
    "dialog:allow-open",
    "dialog:allow-save",
    // 剪贴板 / 打开外部
    "clipboard-manager:allow-read-text",
    "clipboard-manager:allow-write-text",
    "opener:allow-open-url",
    "opener:allow-reveal-item-in-dir",
    // 窗口状态 / 日志
    "window-state:default",
    "log:default"
  ]
}
```

### 9.2 Rust 命令示例（文件读写）

```rust
// src-tauri/src/commands/file.rs
#[tauri::command]
pub async fn read_file(path: String) -> Result<String, String> {
    std::fs::read_to_string(&path).map_err(|e| e.to_string())
}

#[tauri::command]
pub async fn write_file(path: String, content: String) -> Result<(), String> {
    std::fs::write(&path, content).map_err(|e| e.to_string())
}
```

```rust
// src-tauri/src/lib.rs 注册命令
#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_fs::init())
        .plugin(tauri_plugin_dialog::init())
        .plugin(tauri_plugin_clipboard_manager::init())
        .plugin(tauri_plugin_opener::init())
        .plugin(tauri_plugin_store::Builder::new().build())
        .plugin(tauri_plugin_window_state::Builder::default().build())
        .invoke_handler(tauri::generate_handler![
            commands::file::read_file,
            commands::file::write_file,
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

### 9.3 前端调用

```ts
import { invoke } from "@tauri-apps/api/core";
const content = await invoke<string>("read_file", { path: "/abs/path.md" });
await invoke("write_file", { path: "/abs/path.md", content });
```

### 9.4 常用命令

```bash
npm run tauri add fs            # 添加插件（自动更新 Cargo.toml 与注册）
npm run tauri add dialog
npm run tauri dev               # 开发调试
npm run tauri build             # 打包发布
```

---

## 10. 风险与挑战

| 风险 | 影响 | 应对 |
|---|---|---|
| **装饰器逻辑复杂**（6.1 是核心工作量） | 决定"像不像 Typora" | 优先做高频语法（标题/粗斜体/列表/代码块），表格等后置；参考 Obsidian 开源实现思路 |
| **系统 WebView 差异**（尤其 Linux webkit2gtk） | 渲染/快捷键不一致 | 用 Tauri 官方推荐的测试矩阵；CSS 保持标准兼容；尽早三平台联测 |
| **PDF 导出保真度** | 导出效果差 | 先用"打印到 PDF"（复用引擎，保真高），Pandoc 作进阶 |
| **性能**（公式/图表/大文档） | 长文卡顿 | 懒渲染 + 视口虚拟化 + 增量解析（6.3） |
| **权限配置遗漏** | 功能静默失败 | 所有 IO 命令走查 capabilities；开发期打开 `RUST_LOG` 日志 |
| **范围蔓延** | 做不成 Typora 反而做成了别的 | 严格守住"单文件本地 Markdown + 无缝预览"定位（1.2） |

---

## 11. 平台差异设计（Windows / macOS）

> 本项目**只面向 Windows + macOS 双平台**。二者在「窗口头部（标题栏）」与「设置入口」上有本质差异，必须在架构上做平台分支，不能一套布局通吃。

### 11.1 平台检测

统一用 `@tauri-apps/plugin-os` 判断平台，前端据此切换布局：

```ts
import { platform, type } from "@tauri-apps/plugin-os";
// platform(): 'darwin' | 'win32' | 'linux' | ...
// type():     'macOS' | 'Windows_NT' | 'Linux'

const isMac = platform() === "darwin";
```

Rust 侧可用编译期分支：`cfg!(target_os = "macos")` / `cfg!(target_os = "windows")`。

### 11.2 窗口头部（标题栏）

| | macOS | Windows |
|---|---|---|
| 窗口控制按钮 | 原生**红绿灯**，左上角 | 原生**最小化/最大化/关闭**，右上角 |
| 菜单栏 | 屏幕**顶部原生菜单栏**（应用菜单） | 无全局菜单栏 |
| 推荐方案 | `titleBarStyle: "Overlay"` + `hiddenTitle: true`，红绿灯悬浮在应用头部，头部左侧预留 ~76px 安全区 | `decorations: true`（原生标题栏，最稳），应用头部在标题栏之下 |

- **拖拽区**：给头部元素加 `data-tauri-drag-region` 作为窗口拖拽把手；其内部可交互控件（按钮/输入框）加 `data-tauri-drag-region="false"` 排除。
- **macOS 安全区**：`isMac` 时给头部加 `padding-left: 76px`，避免内容压住红绿灯。
- **V1 取舍**：Windows 走原生标题栏（零风险）；macOS 用 Overlay 让头部与内容融为一体。若后续想统一「自定义标题栏」，再对 Windows 用 `decorations: false` + 自绘三键（P2）。

### 11.3 设置入口

- **macOS**：设置放在**应用菜单**（`App > 设置…`，快捷键 `⌘,`），符合平台惯例。用 Tauri 2 **Menu API**（Rust 侧 `tauri::menu`）构建：菜单项 → `emit` 事件 → 前端打开设置面板。
- **Windows**：没有全局菜单栏，设置在**应用内**提供入口（头部右侧齿轮按钮，或「文件 > 设置」）。前端用 `isMac` 决定是否隐藏应用内入口（macOS 上由菜单栏触发）。

> 统一抽象：定义 `openSettings()` 前端事件；macOS 由菜单项触发，Windows 由应用内按钮触发，两条路汇入**同一个设置面板组件**。

### 11.4 文件关联（设为 .md 默认打开程序）

**目标**：把本应用设为 `.md` 的默认打开方式，双击 `.md` 直接在本应用打开。

**（1）打包声明文件关联** —— `tauri.conf.json`：

```jsonc
{
  "bundle": {
    "fileAssociations": [
      {
        "ext": ["md", "markdown"],
        "name": "Markdown",
        "description": "Markdown 文档",
        "role": "Editor",           // macOS: CFBundleDocumentTypes role
        "mimeType": "text/markdown"
      }
    ]
  }
}
```

- macOS：映射为 `Info.plist` 的 `CFBundleDocumentTypes`。
- Windows：NSIS/MSI 安装时写入注册表（`HKEY_CLASSES_ROOT\.md`）。

**（2）接收「打开文件」事件**（关键是**单实例**：已运行时把文件交给现有实例，而不是再开一个窗口）：

- 依赖 `tauri-plugin-single-instance`。
- macOS 另走 `openFile` 事件（`RunEvent::Opened`）；Windows/Linux 从命令行参数 `std::env::args()` 取路径，再转发给主实例。

```rust
tauri::Builder::default()
  .plugin(tauri_plugin_single_instance::init(|app, argv, _cwd| {
      // 第二实例被唤起 → 把 argv 里的 .md 路径转发给主实例
      if let Some(path) = argv.iter().find(|a| a.ends_with(".md")) {
          let _ = app.emit("open-file", path);
      }
  }))
  // ...
```

**（3）「设为默认」运行时**：

- **V1 引导式**：提供「设为默认打开程序」按钮，引导用户到系统设置（Windows「设置 > 应用 > 默认应用」；macOS「文件 > 显示简介 > 打开方式」，或系统设置）。
- **V2 进阶（P2）**：Windows 写注册表 / 调 `assoc`；macOS 调 `LSSetDefaultRoleHandlerForContentType`（需 Objective-C 桥接）。

> 注意：macOS 上「设为默认」必须经系统授权（用户手动或 API），无法静默抢占，UI 需正确引导。

---

## 12. UI 设计原则（干净、传统、去「AI 感」）

> 目标视觉：像 **VS Code / Typora / 系统原生应用**那样克制、专业，而非常见的「AI 生成界面」套路。

**明确禁止的「AI 感」元素：**

- ❌ 渐变紫/蓝/彩色按钮（AI 产品常见）
- ❌ 玻璃拟态、超大圆角卡片、弥散阴影、霓虹光晕
- ❌ emoji 当图标、花哨插画、loading 动画、五彩主题
- ❌ 大留白 + 巨型标题的「落地页」式布局
- ❌ 过度动效（弹性、回弹、悬浮粒子）

**必须遵守的规范：**

| 维度 | 规范 |
| --- | --- |
| 配色 | 中性灰阶（`--gray-50…900`）+ **单一强调色**（跟随系统 Accent），亮/暗两套主题 |
| 字体 | 系统字体栈：`-apple-system, "Segoe UI", system-ui, sans-serif`；等宽 `ui-monospace, "SF Mono", Consolas, monospace` |
| 密度 | 紧凑：侧边栏行高 ~32px、文件树缩进 16px/层，像传统编辑器 |
| 图标 | 细线单色图标（SVG，16–20px），不用彩色 emoji |
| 交互 | 悬停/选中用**背景/边框微变化**（1px 边框或 4% 灰底），不用大阴影 |
| 边框 | 1px 浅色边框分隔区域（`--border`），代替阴影 |
| 圆角 | 小圆角（2–6px），窗口/面板用直角或极小圆角 |
| 动效 | 极少、极快（≤150ms ease-out），不做弹跳 |

**落地方式**：颜色/间距/圆角收敛到 CSS 变量（`:root` 亮色 + `[data-theme="dark"]` 暗色），组件只引用变量不写死值——这也为「自定义主题」打基础。

---

## 13. 参考资料

- [Tauri 2 官方文档 / 插件](https://tauri.app/plugin/) — 插件与权限系统
- [Tauri 2.0 正式版发布](https://tauri.app/blog/tauri-20/) — 新特性与性能对比
- [CodeMirror 6 文档](https://codemirror.net/docs/) — 状态、视图、装饰器
- [@codemirror/lang-markdown](https://github.com/codemirror/lang-markdown) — Markdown 语言支持 + GFM
- [@lezer/markdown](https://github.com/lezer-parser/markdown) — 增量 Markdown 语法树
- [MarkText 架构文档](https://github.com/marktext/marktext/blob/develop/docs/dev/ARCHITECTURE.md) — Muya 引擎与实时预览实现
- [Milkdown](https://milkdown.dev/) — 备选 WYSIWYG 方案（ProseMirror + remark）
- [md-client](https://explore.market.dev/ecosystems/typescript/projects/lin51kevin-md-client) — Tauri 2 + CodeMirror 6 的 Markdown 编辑器参考
- [pulldown-cmark](https://github.com/pulldown-cmark/pulldown-cmark) — Rust 侧 Markdown 解析（导出可选的快速路径）
- [Typora 官方支持](https://support.typora.io/) — 功能对照与快捷键参考

---

## 附：一句话路线图

> **先打通"纯文本 → CodeMirror 6 → 装饰器实时预览"这条主链路，让打开一个 `.md` 文件就能获得 Typora 的编辑手感；再依次补文件树、公式、表格、导出。** 主链路（6.1）是 80% 的体验，其余是 80% 的工作量。
