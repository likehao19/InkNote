# InkNote application source

The desktop application source lives in this directory. For downloads, screenshots, features, shortcuts, development instructions, and project structure, see the [repository README](../README.md).

```bash
pnpm install
pnpm tauri dev
```

Run `pnpm test` and `pnpm build` before packaging with `pnpm tauri build`.
